/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
      domains: ['images.unsplash.com',"unsplash.com","img-cdn.pixlr.com","plus.unsplash.com"],
    },
}

module.exports = nextConfig
