/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com',"unsplash.com","img-cdn.pixlr.com"],
      },
}

module.exports = nextConfig
